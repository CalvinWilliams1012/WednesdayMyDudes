var Game = {
	/* Game version, not usefull now, might be in the future.*/
	version: 0.01,
	/* Main currency of the game. */
	frogs: 0,

	/* -------------------------------GENERATORS------------------------------- */
	/* */
	clickers: 0,
	/* ------------------------------------------------------------------------ */

	/* This function is the *main* function in which the incremental game increments.
	This function is called by using a setInterval timer and based on what the user has increments there score.*/
	function update() {
		//TODO: update score based on generators
	},

}